name = "cdmx-v6-completion"
main = "src/completion.js"
compatibility_date = "2026-04-20"

[vars]
MAX_TOKENS = "80"
RATE_LIMIT_WINDOW_MS = "3600000"
RATE_LIMIT_PER_IP = "6"
FALLBACK_TEXT = "... the difference is not the tool. The difference is the attention."

# Bind ANTHROPIC_API_KEY as a secret before deploy:
#   wrangler secret put ANTHROPIC_API_KEY
